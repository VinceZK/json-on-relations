import {AttributeBase} from './attribute-base';

export class AttributeDropdown extends AttributeBase<string> {
  controlType = 'dropdown';
  options: {key: string, value: string}[] = [];

  constructor(options: {} = {}) {
    super(options);
    this.options = options['options'] || [];
  }
}
